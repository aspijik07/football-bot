{
  "name": "football-broadcast-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p dist && cp index.html matches.json fetcher.js server.js dist/",
    "start": "node server.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
